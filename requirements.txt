# Core requirements (demos + AM grid)
numpy>=1.24.0
matplotlib>=3.7.0
h5py>=3.8.0
tqdm>=4.65.0
torch>=2.0.0
PyYAML>=6.0  # needed by scripts/improving_am/run_grid.py

# Optional: analysis/plots
# scipy>=1.10.0
# seaborn>=0.12.0
# pandas>=2.0.0

# Optional: experiment tracking (not needed for demos/grid)
# mlflow>=2.8.0
# tensorboard>=2.15.0
# wandb>=0.16.0

# Optional: model export/graph visualization
# onnx>=1.18.0
# torchviz>=0.0.2
# graphviz>=0.20.0

# Optional: legacy/archived Lucent integration (not used by current AM engine)
# torch-lucent>=0.1.8 